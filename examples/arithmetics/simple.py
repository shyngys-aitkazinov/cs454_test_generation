from __future__ import annotations

class add:

    def __call__(self, x: int, y: int) -> int:
        return x + y


class substract:

    def __call__(self, x: int, y: int) -> int:
        return x - y

